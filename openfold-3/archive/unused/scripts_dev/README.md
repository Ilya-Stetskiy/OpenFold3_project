This folder stores `openfold-3/scripts/dev` utilities that currently have no
incoming references from active runtime code, tests, notebooks, shell entrypoints,
or project documentation inside `OpenFold3_project`.

They are preserved for manual recovery, but are not part of the supported
runtime or benchmark surface of the consolidated project.
