# OpenFold3 Length Benchmark

Notebook-oriented experiment harness for measuring how OpenFold3 RMSD changes with protein length.

Main entry points:

- `preview_entries(...)`
- `run_length_benchmark(...)`
- `build_notebook_controls(...)`

Artifacts are written under `openfold3_length_benchmark/runs/<timestamp>/`.
