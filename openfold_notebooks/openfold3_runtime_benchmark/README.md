# OpenFold3 Runtime Benchmark

Notebook-oriented profiling harness for OpenFold3 runtime experiments.

Main entry points:

- `preview_entries(...)`
- `run_runtime_benchmark(...)`
- `build_notebook_controls(...)`

Artifacts are written under `openfold3_runtime_benchmark/runs/<timestamp>/`.
